//Instructions
// This code does not execute properly. Try to figure out why.
//Solution
function multiply(a, b){
//P - parameters takes in two variables of int types
//R - an integer that is the total of a by b
//E - (3,4), (12))
//P - uses the * symbol to multiply a * b and returns using the return statement
    return a * b
   }