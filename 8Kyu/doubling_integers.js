//Instructions
//Code as fast as you can! You need to double the integer and return it.

//Solution
// i an integer
// return i doubled in integer format
// ((3) 6)
// uses the * operator to double i and return the appropriate integer
function doubleInteger(i) {
    // i will be an integer. Double it and return it.
    return i * 2;
  }